import { default as WebSocket } from 'ws';
export declare type HostType = 'app' | 'companion';
export declare function createHostConnection(hostType: HostType): Promise<WebSocket>;
export declare function createDebuggerHost(socket: WebSocket, hostType: HostType, handleLog: (msg: string) => void, hostProperties: {
    maxAPIVersion?: string;
}): Promise<void>;
