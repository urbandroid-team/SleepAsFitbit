import { Pixel } from './types';
export default class TZ1200RLE {
    private lastPixel?;
    private pixelCount;
    private sectionIndex;
    private willCompress;
    private section;
    constructor(bytesPerPixel: number);
    private writePixelToSection(pixel);
    private readonly isSectionFull;
    flush(): Uint8Array | null;
    private internalFlush();
    encode(pixel: Pixel): Uint8Array | null;
}
