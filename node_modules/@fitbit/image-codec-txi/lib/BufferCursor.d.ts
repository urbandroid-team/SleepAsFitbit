export default class BufferCursor {
    length: number;
    private p;
    private view;
    buffer: ArrayBuffer;
    constructor(length: number);
    seek(offset: number): void;
    tell(): number;
    slice(from?: number, to?: number): ArrayBuffer;
    writeUInt8Array(arr: number[] | Uint8Array): void;
    writeUInt32LEArray(arr: number[] | Uint32Array): void;
}
