"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const fs_1 = tslib_1.__importDefault(require("fs"));
const path_1 = require("path");
const mimeTypes = {
    '.jpg': 'image/jpeg',
    '.jpeg': 'image/jpeg',
    '.png': 'image/png',
    '.gif': 'image/gif',
};
function resourceImports(options = {}) {
    return {
        name: 'resourceImports',
        load(id) {
            const mime = mimeTypes[path_1.extname(id)];
            if (!mime)
                return undefined;
            const file = fs_1.default.readFileSync(id);
            return `export default "data:${mime};base64,${Buffer.from(file).toString('base64')}";`;
        },
    };
}
exports.default = resourceImports;
